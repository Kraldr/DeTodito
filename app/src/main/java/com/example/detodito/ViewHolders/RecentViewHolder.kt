package com.example.detodito.ViewHolders

import android.content.Context
import android.view.View
import androidx.recyclerview.widget.RecyclerView
import com.example.detodito.databinding.ItemSearchRecentCategoriesBinding


class RecentViewHolder (view: View): RecyclerView.ViewHolder(view) {

    private val binding = ItemSearchRecentCategoriesBinding.bind(view)

    fun bin(item: String, contexto: Context) {

    }
}