package com.example.detodito.ViewHolders

import android.view.View
import androidx.fragment.app.FragmentActivity
import androidx.recyclerview.widget.RecyclerView
import com.example.detodito.databinding.ItemOfertsBinding


class OfertsViewHolder (view: View): RecyclerView.ViewHolder(view) {

    private val binding = ItemOfertsBinding.bind(view)

    fun bin (contexto: FragmentActivity) {

    }
}